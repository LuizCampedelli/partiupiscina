<h1>Pools</h1>
<div class="container">
  <div class="row row-cols-2">
    <div class="row w-100">
    <% @pools.each do |pool| %>
    <div class="col-6">
      <li><%= pool.name %></li>
      <%= link_to 'SHOW THIS POOL', pool_path(pool.id) %>
    </div>
    <% end %>
    </div>
  </div>
</div>






<%# ESCOLHER UMA PISCINA, e fazer o caminho para a piscina%>
