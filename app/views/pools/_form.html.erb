<%= simple_form_for(pool) do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :size %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.button_button t(".submit"), name: "Confirmar", class: "btn btn-flat" %>
<% end %>
